# compose-md-cli

## 0.1.0

### Minor Changes

- cd43801: Publish `compose-md-cli` to npm. The package now ships the built `dist/cli.js` output and includes changesets tooling and a `release` GitHub Actions workflow that opens a "Version Packages" PR (or publishes to npm) on push to `main`.

### Patch Changes

- 64fbe75: test patch
