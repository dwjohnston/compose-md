---
"compose-md": minor
---

Publish `compose-md` to npm. The package now ships the built `dist/cli.js` output and includes changesets tooling and a `release` GitHub Actions workflow that opens a "Version Packages" PR (or publishes to npm) on push to `main`.
